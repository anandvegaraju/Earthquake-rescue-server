#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@google-cloud/nodejs-repo-tools/bin/samples" "$@"
  ret=$?
else 
  node  "$basedir/../@google-cloud/nodejs-repo-tools/bin/samples" "$@"
  ret=$?
fi
exit $ret
